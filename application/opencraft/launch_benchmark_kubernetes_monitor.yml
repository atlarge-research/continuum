---
- hosts: cloudcontroller
  become: true
  tasks:
    - name: Create job file
      shell: |
        cat > "/home/{{ username }}/monitor.yaml" <<EOF
        apiVersion: batch/v1
        kind: Job
        metadata:
          name: {{ app_name }}
        spec:
          template:
            metadata:
              name: {{ app_name }}
            spec:
              containers:
              - name: {{ app_name }}
                image: {{ image }}
                imagePullPolicy: {{ pull_policy }}
                resources:
                  requests:
                    memory: "{{ memory_monitor }}Mi"
                    cpu: {{ cpu_monitor }}
                env:
                - name: SLEEP_TIME
                  value: "{{ sleep_time }}"
              restartPolicy: Never
              nodeSelector:
                kubernetes.io/hostname: {{ node_monitor }}
        EOF

    - name: Launch job
      command: >
        kubectl apply -f "/home/{{ username }}/monitor.yaml"

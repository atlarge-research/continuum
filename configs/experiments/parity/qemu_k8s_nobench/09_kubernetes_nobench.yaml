---
schema_version: 1
kind: ContinuumExperiment

use:
  environment: local-qemu
  software: k8s-observability-endpoint-runtime

run:
  targets: [infrastructure, software]
  image_prefetch: "off"
  dry_run: false
  clean: false

infrastructure:
  clusters:
    - id: cloud-1
      tier: cloud
      resources:
        vms:
          count: 2
          spec:
            cores: 2
            memory_gb: 4
            cpu_quota: 1.0
    - id: endpoint-1
      tier: endpoint
      resources:
        vms:
          count: 2
          spec:
            cores: 1
            memory_gb: 2
            cpu_quota: 0.5
  network:
    emulation: true
    wireless_preset: 4g
    overrides: {}

---
schema_version: 1
kind: ContinuumSoftware

software:
  modules:
    - id: kubecontrol-main
      type: kubecontrol
      assign_to:
        match:
          cluster: cloud-1
      config:
        kube_version: "v1.27.0"
        kube_deployment: call
        cache_worker: true
